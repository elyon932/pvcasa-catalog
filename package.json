{
  "name": "pvcasa-catalog",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "lint": "eslint .",
    "build": "node scripts/build.mjs",
    "deploy": "npm run build && firebase deploy --only hosting"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0",
    "vitest": "^3.0.0"
  }
}
