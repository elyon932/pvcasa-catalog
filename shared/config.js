// Store settings — edit these values to configure the catalog.
// This is the only file you need to change for day-to-day settings.

// WhatsApp number that receives orders, in international format
// (country code + area code + number, digits only).
export const WHATSAPP_NUMBER = "5593992274444";
